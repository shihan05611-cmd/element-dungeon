# AI 协作中枢规则



> 1. **中枢负责目标到任务的转换。** 将用户需求固化为可独立理解的编号任务书，明确权威来源、依赖、修改边界和验收标准，再交由职责匹配的独立执行对话；人工主要负责目标定义与关键设计决策。
> 2. **任务书是正式协作载体。** 聊天只用于沟通；事实冲突时按“用户当前决定 → 当前有效设计/需求 → 当前任务书 → 已验收产物 → 历史记录 → 聊天记忆”取信。
> 3. **执行与验收分离。** Executor 只能在授权范围内实现和测试，范围外问题只回报；完成后携带证据进入 `REVIEW` 并冻结，不得自行验收或扩大需求。
> 4. **Review 必须重新验证。** 不直接采信 Executor 的交付结论，只依据任务契约、实际改动和测试证据给出 PASS / FAIL；FAIL 退回执行，只有 PASS 后才可 `ACCEPTED`、归档并进入版本基线。
> 5. **上下文不可靠时重新加载。** 任一 Agent 遇到上下文压缩、事实冲突、信息缺失或历史过长时，必须重新读取正式材料，必要时更换独立对话，不凭摘要或记忆继续。
> 6. **禁止互相污染。** 多任务发生修改范围重叠时停止并交由中枢重新调度；不得覆盖来源不明、未验收或其他任务的工作。
> 7. **Git 与恢复操作受控。** Executor 默认不执行 Git 写操作和破坏性恢复；仅在验收完成并获得授权后建立版本检查点。
> 8. **Agent 自主回传。** 工具允许时，执行与 Review 直接向中枢回传状态和证据，中枢主动跟踪，不让用户承担跨对话的信息搬运。
> 9. **推理资源按职责配置。** 中枢默认使用最高可用推理强度，以保证拆解、边界和全局决策质量；执行与 Review 根据任务复杂度和风险配置。
> 10. **质量优先。** 不为降低 Token、时间或调用成本省略必要实现、测试和验收。
