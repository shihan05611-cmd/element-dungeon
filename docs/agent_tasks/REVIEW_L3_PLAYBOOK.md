# 已停用：Review L3 手册

项目不再使用强制 L1/L2/L3 分级或默认的 L3 重型验收流程。

现行 Review 规则见 `docs/agent_tasks/REVIEW_AGENT_RULES.md`：Review Agent 按实际风险选择最小充分验证，其 `PASS` 即为最终接受。只有当前任务确实需要时，才自行增加冷副本、全量回归、性能复测或证据包等手段。

旧手册原文仅保存在 `docs/_discarded/agent_rules_legacy_20260820/docs_agent_tasks/REVIEW_L3_PLAYBOOK.md.disabled`，不再具有约束力。
