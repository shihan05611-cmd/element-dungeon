# ImageGen 生成记录

模式：Codex 内置 `image_gen`（默认模式）。两张有效 v2 图均为方向确认用概念稿，不是运行时资源。

## 当前有效局部稿（需求降级）

有效文件：`status_hud_water_scoped_concept.png`、`status_hud_fire_scoped_concept.png`。

水态最终提示要求：只输出左上角状态栏局部组件；保留两行 HP/SP 的比例、条长与文字，删除右侧独立元素卡片并收紧空间。水态通过内收波阶、低对比水平涟漪和与右边框连成一体的小水滴缺口识别。画面禁止出现技能槽、被动槽、Boss、房间信息、角色或场景。

火态最终提示要求：以水态局部稿为输入，只替换边框主题；逐项锁定组件宽高、两行结构、条长、文字位置和功能色。波阶改为同占位焰阶，水平纹样改为上升火星，水滴缺口改为同尺寸火苗缺口；HP 保持珊瑚红，SP 保持青蓝。禁止出现其他 HUD。

下方“基线校正 / 水态 / 火态”记录属于需求降级前的整屏稿，仅留作追溯。

## 基线校正

初稿误用了 task61 的旧 HUD 截图，错误地把被动槽保留在右上。用户指出后，重新核对当前 `scripts/combat_hud.gd`：`PassivePanel` 与 `SkillPanel` 都使用 `PRESET_CENTER_BOTTOM`，被动条位于主动条上方。有效稿因此改用 task75 当前截图作为权威布局参考；task61 只提供 Boss 信息层级，不提供槽位位置。

## 水态

输入参考：Image 1 为 `docs/agent_tasks/evidence/task75/screenshots/task75_05_battle02_tidal_sentry_live_projectile_1920x1080.png`（权威布局）；Image 2 为 task61 Boss 截图（只参考 Boss 内容层级）。

最终提示要求：以 task75 当前 1920×1080 战斗截图为结构权威，保留左上 HP/SP、其下房间信息、顶部 Boss，并强制四被动槽位于底部中央、三主动槽正上方；禁止顶部或右上出现被动槽。彻底删除 HP/SP 右侧独立元素枢纽且不留空洞；制作简约硬边像素 UI。水态使用内收波阶、水平涟漪和与既有边框相连的小型水滴标记；HP、SP、冷却、危险与文字功能色保持语义稳定。

## 火态

输入参考：`hud_water_concept_v2_bottom_passives.png` 的生成原图。

最终提示要求：只将 v2 水态的主题身份改为火态，逐项锁定截图、面板位置、宽高、内边距、槽位数量、条形长度、文字位置、场景与角色不变；尤其锁定四被动槽在底部中央、主动槽上方。波阶改焰阶、水平涟漪改上升火星、水滴标记改同尺寸分叉火苗标记，主题强调色改为橙 / 金；HP 保持珊瑚红、SP 保持青蓝；不增加独立元素卡片或空占位。
