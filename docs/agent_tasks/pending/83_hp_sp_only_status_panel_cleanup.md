# 状态栏收口为纯 HP/SP

建议：gpt-5.6-terra / medium

Allowlist：战斗 HUD 的左上 `StatusPanel` 构建、绑定与尺寸布局模块；仅用于移除元素枢纽/旧标题分支的 HUD 主题资源引用；直接受影响的 HUD 自动化测试、窗口渲染脚本与 `docs/agent_tasks/evidence/task83/`。当前元素的权威状态、切换与技能可用性逻辑，主动/被动技能区、Boss 栏、房间信息、反馈层、RunOverlay、配装/商店及其他战斗系统均不得修改。

目标：元素枢纽最终不显示，左上状态栏只保留血条与蓝条；去掉右侧空位和一切元素图标、文字、色块、主题切换痕迹，不改变元素玩法与其他 HUD。

范围：从状态栏节点树与公开接口中完整移除可见 `CurrentElement` 以及已隐藏的 `TitleRow`、`ElementBadge`、元素色块/形状/文字等遗留分支，不以继续隐藏代替清理；删除只服务于这些分支的构建、绑定、刷新和尺寸常量。`StatusPanel` 使用统一简约像素风面板，收紧到容纳 HP/SP 两行的宽度，不保留元素槽空白；保留既有左上锚点、HP/SP 路径、数值刷新和低血量提示。元素权威状态、形态切换、技能槽可用性、反馈与 RunOverlay 中必要的元素信息继续正常工作。若工作区仍有 Task 80 的状态栏主题资源或引用，仅在确认无其他消费者后移除，不重新实现水火两套状态栏。

需改断言：Agent D 集成中的玩家标题断言改为纯 HP/SP 结构断言；HUD 配装反馈中的隐藏元素徽章文字断言改为元素节点不存在；Task 24 的元素枢纽父级、尺寸、形状、文字及无障碍颜色断言改为纯 HP/SP 和切换模式不复生元素 UI；Task 30 的元素形状/文字与色盲保留形状断言改为 HP/SP 数值存在、状态栏结构和几何稳定；Task 74 的元素枢纽专项改为 `StatusPanel` 仅含 HP/SP 与低血量呈现；Task 30 窗口 capture 删除元素色块/形状/文字像素门槛，补齐 HP、SP 两行的可见性与几何门槛。仅依赖 `CombatHUD.STATUS_SIZE` 的拖拽/密度断言应随常量自动适配，不改写为旧固定宽度；TargetPanel、RunOverlay 和玩法层的元素断言不属于本任务，不得删改。

完成：运行时 `StatusPanel` 不存在 `TitleRow`、`CurrentElement`、`ElementBadge`、`ElementSwatch`、`ElementShape` 或 `ElementText`，只呈现 HP、SP 及既有低血量反馈；面板无右侧空槽，水火形态切换前后状态栏外观与几何不变，HP/SP 数值正确刷新。窗口同机位证据覆盖两种元素形态和低血量状态；上述断言完成定向迁移，未受影响的 HUD、切形态、技能可用性与正式回归全部通过。
