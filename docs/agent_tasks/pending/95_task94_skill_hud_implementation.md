# Task 95 Task94 技能 HUD 正式实装

建议：gpt-5.6-sol / high

Allowlist：技能 HUD 的布局、运行时表现与相关辅助 UI；`assets/ui/hud_skill/` 正式贴图；相关 HUD 聚焦测试与捕获脚本；`docs/agent_tasks/evidence/task95/` 验证证据。Task94 最终概念证据只读。共享文件中的 Task91 状态 HUD和现有元素技能图标渲染改动必须原样保留。

目标：把用户确认且独立 Review PASS 的 Task94 最终技能 HUD 概念实装到 Godot：主动三槽位于底部中央，共用连续厚外框；被动四槽位于右下 `4×1`；保留现有技能、冷却、能量、元素禁用、被动触发和 H 键显隐行为。

范围：

- 视觉权威按以下顺序：`task94_skill_hud_hierarchy_states_full_concept_1920x1080.png`、`task94_active_hud_closeup_4x_nearest.png`、`task94_passive_4x1_closeup_4x_nearest.png`、`task94_state_rules_1920x1080.png`、更新后的 Task94 任务书。Task94 说明文件中若仍存在“独立闭合槽框/内层矩形”等返工前残留文字，一律视为作废，不得据此回退。
- 从 Task94 最终 PASS 图确定性提取、清理并制作静态框体资源；不得重新调用 imagegen、不得代码重绘近似、不得把图标、键位数字、SP 数值、冷却读秒或状态覆盖烘焙进静态贴图。保留资源来源、裁切范围、原始尺寸、alpha 与最近邻处理记录。
- 主动区位于底部中央：3 槽共用一条连续厚外框，顶/底边贯通；内部仅两条竖向槽间分隔，不得出现三个独立闭合内框、成对阶梯接缝或边线终止/重启。
- 主动键位片位于每槽左上；技能图标使用现有真实运行时纹理；`SP N` 位于每槽右下。实际未装备槽保留对应键位片，但不显示图标、SP、冷却或额外占位符。
- 主动冷却保持动态：遮罩按 Task94 规则自底向上擦除/推进，剩余秒数居中；冷却结束后完整恢复图标。元素不匹配、能量不足、忙碌等现有灰度禁用语义不得回归。
- 被动区锚定右下，固定安全边距，准确 `4×1` 横排并向左生长；不得再与主动区连接或回到中央上方。被动框使用同源材质，但线宽、亮度、层次显著弱于主动框。
- 被动槽不显示键位、SP 或文字。未装备槽显示内凹虚线；已装备槽显示真实图标；现有被动 transient/触发反馈映射为细亮边脉冲，不缩放、不闪白。项目当前没有被动槽锁定的玩法权威：实现可复用的“小锁”视觉状态并用聚焦测试/夹具验证，但默认运行流程不得凭空锁槽或改变购买、装备、拖拽和运行时注册规则。
- `set_skill_hud_visible()` / H 键必须同时控制主动区和右下被动区，隐藏时逻辑、冷却与技能施放继续正常运行。
- 保持节点契约或提供兼容访问，使现有技能 HUD、配装、拖拽、截图和可访问性测试能够按意图迁移；不得通过删除断言、放宽布局判据或跳过用例掩盖回归。
- 当前工作区已有未提交的 Task91 状态 HUD、`element_skill_icon_renderer.gd`、`run_element_skill_icon_renderer_tests.gd` 及 `combat_hud.gd`/`run_hud_loadout_feedback_tests.gd` 相关改动。先逐 hunk 检查归属并增量合并；禁止整文件 checkout/reset、覆盖或删除任何现有未提交成果。
- 不修改状态 HUD、Boss HUD、房间文字、RunOverlay、商店/配装玩法、技能定义、战斗数值和元素技能图标内容；不提交、不推送。

完成：

- 正式主动/被动 HUD 贴图可追溯至 Task94 最终 PASS 图，尺寸与 alpha 检查通过，Godot 使用最近邻显示且无模糊拉伸、静态文字残影或烘焙动态内容。
- 新增聚焦测试覆盖：布局/锚点与安全边距、连续主动外框结构、3+4 槽数量、动态键位/SP/空槽、冷却方向与居中读秒、灰度禁用、被动虚线空槽/锁视觉夹具/装备/触发脉冲、H 键显隐及节点兼容。
- 检查并运行受影响的既有 HUD 密度/主题/配装反馈/H 键测试；intentional 布局变化只精确更新对应期望，同时保留并通过 Task91 与元素技能图标的新增测试。
- 使用唯一允许的 `C:\Users\heliashi\Desktop\Godot_v4.7.1-stable_win64.exe\Godot_v4.7.1-stable_win64_console.exe`；除 `--version` 外，每条 Godot 命令显式指定工作区内唯一 `--log-file`。
- 提供真实窗口同机位证据：1920×1080 正常/冷却/空槽/被动触发状态，H 键显示与隐藏；另提供 1152×648 和 2560×1440 布局截图，确认主动居中、被动右下安全边距、无重叠和无裁切。测试夹具专用锁状态必须明确标注，不得冒充正式玩法截图。
- 人工逐图对照 Task94：主动框确为连续统一外框且只有两条分隔；被动确为右下 `4×1` 并弱于主动；键位、图标、SP、冷却和被动状态在原始显示尺寸可读。
- 独立 Review Agent 检查 Allowlist、脏工作区隔离、资源来源、代码/测试和真实窗口证据后给出 PASS；仅自动测试通过不足以验收。
