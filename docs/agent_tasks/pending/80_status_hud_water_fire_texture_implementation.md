# 左上状态栏水火双主题贴图实装

建议：gpt-5.6-terra / medium

Allowlist：战斗 HUD 左上状态栏的构建、主题与当前元素表现模块；HUD 正式像素美术资源目录中本任务新增的状态栏九宫格、暗纹和水滴/火苗徽记；仅与左上状态栏直接相关的 HUD 自动化测试、窗口渲染取证及 `docs/agent_tasks/evidence/task80/`。Task 79 的两张确认概念图为只读依据；主动槽、被动槽、Boss 栏、房间信息、其他界面、战斗逻辑和元素权威状态均不得改动。

目标：把用户确认的 `status_hud_water_scoped_concept.png` 与 `status_hud_fire_scoped_concept.png` 落地为左上 HP/SP 状态栏的正式像素贴图和运行时水火主题切换；移除可见的独立元素卡片，以外框相连的元素徽记表达当前状态。

范围：只改左上 StatusPanel。保持 HP、SP 两行结构、条长关系、数值内嵌、文字基线、左上安全边距和现有数据刷新语义；删除当前右侧独立 CurrentElement 卡片的可见框体及其空白占位，收紧外框但不得扩大当前状态栏占屏面积或挤压房间标题。依据确认稿制作可用于 Godot 的最近邻 PNG/九宫格切片：水态使用蓝色波阶边框、低对比水平涟漪和水滴缺口徽记；火态使用橙色焰阶边框、低对比上升火星和火苗缺口徽记。HP 珊瑚红、SP 青蓝、主文字暖白等功能色在两态保持一致；当前元素同时由颜色、角部轮廓、暗纹方向和徽记剪影识别，不能只换色。水火资源尺寸、拉伸边距、内容边距和徽记外接范围必须完全同构；元素切换只原位替换主题贴图/样式与徽记，不重建节点布局，不产生位移、闪白、插值模糊或状态延迟。概念图不是可直接裁切的最终资产，执行者需按项目像素网格重制正式资源。

完成：启动时状态栏立即匹配玩家当前元素，水火切换后在同一帧或既有元素变更刷新点正确替换边框、暗纹和徽记；两态外框尺寸、HP/SP 几何与文字基线逐像素一致，独立元素卡片及其可见占位完全消失，仍可凭轮廓和徽记明确识别水/火。主动槽、被动槽、Boss 栏、房间信息和战斗行为与任务前一致。提供同一分辨率、同一数值、同一机位的水态/火态窗口渲染对照，以及低血量、非满 SP 和连续切换后的状态图；自动化验证覆盖主题初始化、实时切换、几何不变、最近邻导入、状态栏不越界/不重叠及旧元素卡片不可见，并通过相关正式 HUD 回归。
