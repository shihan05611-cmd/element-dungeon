# Review Docs Agent 2.0 文档任务：当前玩法交接稿规则对齐

状态：ACCEPTED
负责人：Review Docs Agent 2.0
协调线程：`019fb360-a85c-77a0-b347-17e268eca03e`（Review Docs 2.0：玩法交接稿对齐）
依赖：11_agent_f_architecture_slimming 全部阶段 Review 验收通过
可并行性：不得在 Agent F 第一至第三阶段期间提前修改

## 1. 任务定位

更新 `docs/current_gameplay_design_handoff.md`，使它与已经冻结并实现的共享技能栏、CurrentElement 和成长系统一致。

本任务只修正文档中的过时事实，不新增策划规则，不修改代码。

## 2. 已知冲突

当前交接稿仍包含以下旧方案：

- 水、火形态分别拥有独立技能槽映射。
- `SkillLoadout` 按形态保存两套配置。
- 主要技能只描述为单个 Q 槽。
- 后续问题仍在讨论“双形态独立技能槽数量”。

这些内容与当前冻结规则冲突。

## 3. 更新目标

- 改为全元素共享唯一 `ACTIVE_1～3 + PASSIVE_1`。
- 普通攻击明确位于共享技能栏之外。
- 元素切换不更换已装备技能。
- 三类技能统一为 EXCLUSIVE_ELEMENT、CURRENT_ELEMENT、NEUTRAL。
- 专属技能仅在释放接受后自动切换元素。
- 通用技能和已经生成的攻击使用锁定元素快照。
- ACTIVE 位置可装备被动，PASSIVE_1 拒绝主动，`0 主动 + 4 被动` 合法。
- 输入说明与当前三个主动动作、独立普通攻击和独立元素切换一致。
- 补入已完成的角色等级、属性点、奖励、遗物、商店和正式 RunSessionHost 数据流概况。
- 删除或改写已经定稿的旧开放问题，不把冻结规则继续描述成待讨论方案。

## 4. 禁止范围

- 不改 `docs/design/共享技能槽与元素释放规则.md` 的冻结规则。
- 不修改战斗、成长、UI 或场景文件。
- 不借文档更新调整数值和玩法方向。
- 不提前猜测 Agent F 尚未验收的最终类名和公共 API。

## 5. 验收

- 全文搜索不再把水火独立 Loadout 描述为当前规则。
- 技能槽、元素切换、锁定快照和成长闭环与任务 10/11 验收后的实现一致。
- “当前实现”“冻结规则”“后续待讨论”三类内容边界清晰。
- 报告所有改写章节，不执行 Git 命令。

## 6. 协调者正式下发（2026-07-30）

- 任务 11 与 14～18 已全部 `ACCEPTED`；交接稿只写已冻结、已实现且已验收的事实，不预写任务 12 尚未验收的最终 UI。
- 本任务与任务 12 并行，但文件严格互斥：只允许修改 `docs/current_gameplay_design_handoff.md` 与本任务文档。
- 已新建独立文档任务并直接下发。完成后只更新为 `REVIEW`，向协调线程回传；不得修改代码、资源、场景、测试或执行 Git。

## 7. Review Docs Agent 2.0 交付记录（2026-07-30）

- 已完整读取本任务书、当前交接稿、《共享技能槽与元素释放规则》、任务 10/11/14～18 已验收记录及 `docs/agent_tasks/README.md`；仓库内未发现适用的 `AGENTS.md`。
- 已重写交接稿的阅读口径、当前闭环、元素与伤害、共享技能栏、`CurrentElement`、释放快照、正式技能内容、成长与本局数据流、旧配置迁移、表现/UI 边界、开放问题及网页端提示词。
- 当前规则已统一为唯一 `ACTIVE_1～3 + PASSIVE_1`，固定普通攻击独立；三类元素策略、接受后自动切换、锁定快照、ACTIVE 装被动、PASSIVE 拒绝主动及 `0 主动 + 4 被动` 均按冻结规则表述。
- 已补入任务 10/11/14～18 验收后的角色等级、属性点、奖励、遗物、拥有技能库、共享商店换装、正式内容目录和房间级 `RunSessionHost` 数据流。
- 已明确首批六个可获取技能与固定普通攻击的当前目录状态；`water_lance`、`fire_lance` 和四个旧属性被动只标为 legacy/test fixture，不再描述为正式双元素配装。
- 已单列任务 12 边界：只保留冻结 UI 契约，不猜测最终 HUD、配装布局、键帽或反馈文案。
- 旧术语检查结论：交接稿中不再把“双形态独立配装”“两套当前配置”或“单 Q 槽”描述为现行方案；相关字样仅以否定旧方案、历史迁移输入或网页端禁止恢复旧方案的上下文出现。
- 残留开放问题共七项：后续元素内容顺序、普通攻击是否元素化、角色元素表现扩展、攻击/施法速度、附着衰减/持续时间/抗性、未来版本的反应平衡评审，以及任务 12 最终 UI Review。它们均明确不改变当前冻结规则。
- 本任务为纯文档更新；未修改代码、场景、资源或测试，未执行任何 Git 命令，未自行标记 `ACCEPTED`。

## 8. Review 最终验收（2026-07-30）

- 文件边界通过：任务运行期间仅 `docs/current_gameplay_design_handoff.md` 与本任务文档发生对应修改，未发现代码、场景、资源或测试变更。
- 协调者逐段核对任务 10/11/14～18 已验收事实；共享 `ACTIVE_1～3 + PASSIVE_1`、独立普通攻击、三类元素策略、接受事务、锁定快照、成长/奖励/遗物/ShopDraft/RunSessionHost、首批六技能和任务 18 表现描述均有归档依据。
- 全文旧术语检查通过：“按元素拆分的两套当前配置”只用于否定旧方案，旧元素独立配置只位于一次性迁移章节；交接稿不再把双形态 Loadout 或单 Q 槽描述为当前规则。
- “当前实现”“冻结规则”“仍待讨论”边界清晰；任务 12 只记录冻结 UI 契约，未把尚未验收的布局、键帽、交互或文案写成现状。
- 开放问题均明确需要另立后续评审，不改变当前冻结公式、槽位、快照或数据流。
- 结论：任务 13 通过 Review，状态更新为 `ACCEPTED` 并归档。