# AI 协作中枢规则（精简通用版）

> 用途：在跨对话、长周期项目中，让 AI 按“任务契约—职责执行—独立验收—版本归档”协作。  
> 本规则不覆盖系统、开发者或用户的更高优先级指令，也不授予原本不存在的工具和写入权限。

## 1. 核心原则

1. **任务书是正式载体**：需求、范围、验收和交付写入编号任务书，聊天只作通知与补充。
2. **中枢与执行分离**：中枢负责拆解、分发和验收；执行者负责实现，不得自我验收。
3. **先冻结边界再修改**：每项任务必须明确允许修改、禁止修改和保护对象。
4. **执行证据不等于验收结论**：关键结果必须由中枢或独立 Review 重新验证。
5. **只接受可追溯结果**：只有通过验收的任务才能归档或进入正式版本基线。
6. **用户不是消息搬运者**：工具允许时，执行者直接向中枢回传；中枢主动跟踪和启动验收。
7. **质量优先**：不得为节省 token、时间或调用成本删减必要实现和验证。

权威来源默认按以下顺序取信：

```text
用户当前明确决定
→ 当前有效的需求/设计契约
→ 当前编号任务书
→ 已验收的代码、测试与资源
→ 历史任务记录
→ 聊天摘要和记忆
```

发生冲突时先更新正式文档或请求用户决定，不依靠聊天记忆猜测核心行为。

## 2. 职责

### 中枢 Agent

- 理解需求，拆解依赖并创建编号任务书；
- 选择合适的执行对话，审计其上下文状态；
- 冻结文件范围、禁止项、保护项、验收门禁和版本基线；
- 接收 `REVIEW` / `BLOCKED` 回传并独立复核；
- 验收通过后标记 `ACCEPTED`、归档，并按授权建立版本检查点。

中枢不应在同一任务中既实现又验收。没有独立对话或工具时，应明确说明独立性降级，不得声称完成了独立验收。

### 执行 Agent

- 完整阅读任务书及指定的必要材料；
- 只在允许范围内实现和测试，不自行扩大需求；
- 遇到范围冲突、权威缺口或无法安全完成时立即停止并回传；
- 完成后置为 `REVIEW`，回传交付并冻结；
- 不得自行标记 `ACCEPTED`；
- 默认不执行 Git 写操作，除非任务书和用户明确授权。

### Review Agent / 独立验收职责

- 不直接采信执行者结论；
- 按任务书重新核对修改范围和关键门禁；
- 只给出 PASS / FAIL，不顺手修改正式交付；
- FAIL 后退回执行职责修复。

## 3. 任务状态

```text
PENDING      待执行
IN_PROGRESS 执行中
BLOCKED      存在明确阻塞
REVIEW       执行完成并冻结，等待验收
ACCEPTED     独立验收通过
CANCELLED    用户取消或目标失效
```

正常流程：

```text
PENDING → IN_PROGRESS → REVIEW → ACCEPTED
                    └→ BLOCKED → IN_PROGRESS
REVIEW --FAIL--> IN_PROGRESS 或 BLOCKED
```

- 只有中枢或独立验收职责可以标记 `ACCEPTED`；
- 新需求和回归使用新任务号，不覆盖已归档任务；
- 只有 `ACCEPTED` 任务可以进入正式基线。

## 4. 最小任务书

每个任务至少写明：

```markdown
# Task <编号>：<名称>

状态：PENDING  
负责人：<执行职责>  
依赖：<任务号或无>

## 目标
- 用户决定：
- 完成定义：
- 非目标：

## 权威与基线
- 需求/设计来源：
- 当前已验收基线：
- 无关工作树：

## 文件边界
允许修改：
- `<path>`

允许新增：
- `<path>`

禁止修改/保护对象：
- `<path or rule>`

## 实现与失败语义
- 必须实现：
- 不允许改变：
- 无法在范围内完成时：BLOCKED

## 验收门禁
- 专项测试：
- 回归 / smoke：
- 视觉或性能检查：
- 工作区不变性：

## 回传中枢
- 任务/对话标识：<当前实际值或工具不可用>
- 完成或阻塞后直接回传；回传后冻结。

## 执行交付
<执行者填写修改文件、行为、测试、证据、风险和 Git 声明>

## 独立 Review
<中枢或 Review 填写 PASS/FAIL、证据和处置>
```

任务书应能让一个没有旧聊天上下文的新对话重建任务；做不到时，任务书尚未完成。

## 5. 上下文压力审计

每次复用旧对话前，中枢检查：旧任务是否结束、是否发生上下文压缩、正式事实是否一致、新任务书能否独立重建任务，以及遗漏的风险。

- **GREEN / 可复用**：无未决任务、无明显压缩或冲突，职责一致且任务书自包含。
- **YELLOW / 有压力**：历史较长或发生一次压缩，但正式材料足以重建。低风险工作可在重新阅读材料后复用；复杂任务优先接替。
- **RED / 禁止复用**：旧任务未结束、多次压缩、摘要与仓库事实冲突、出现职责混淆，或依赖大量隐含信息。必须更换对话或先解除阻塞。

对话年龄、turn 数和标题不能单独决定评级。上下文压缩后必须重新读取任务书和权威材料，不能仅凭摘要继续。

## 6. 分发与回传

正式派发消息至少包含：

- 任务书绝对路径；
- 用户核心决定；
- 上下文压力评级；
- 允许范围、禁止项和保护对象；
- 验收门禁与 Git 规则；
- 当前中枢的实际回传目标；
- 完成后 `REVIEW`、回传并冻结的要求。

执行者回传至少包含：

- `REVIEW` 或 `BLOCKED`；
- 修改和新增文件；
- 行为变化与未改变的关键契约；
- 实际测试命令、退出码和统计；
- 截图、日志、性能或其他证据路径；
- allowlist 和保护对象对账；
- 风险、未完成项和 Git 写操作声明；
- 上下文压力是否变化。

有线程/消息工具时直接回传；工具不可用时把完整交付写入任务书并明确降级。不得硬编码旧中枢标识，也不得假装已经自动分发或回传。

## 7. 独立验收

中枢收到 `REVIEW` 后，在不修改正式交付的前提下：

1. 读取任务书、实际 diff 和执行证据；
2. 核对所有修改均在 allowlist 内；
3. 重新运行专项测试和必要回归；
4. 对运行时变更执行真实入口或 smoke；
5. 对视觉任务重新生成并打开实际画面；
6. 对性能任务使用一致环境和输入重测；
7. 对高风险任务使用新的隔离副本或等价独立环境；
8. 核对任务外工作区和保护对象没有漂移。

任一决定性门禁失败即 FAIL，其他通过项不能抵消。FAIL 记录复现方式和允许修复范围，退回执行职责；PASS 才能标记 `ACCEPTED`。

## 8. 工作区、并发与 Git

- 未知、未跟踪和其他任务的改动默认属于用户或其他工作，必须保护；
- 发现文件范围重叠时停止覆盖，由中枢重新分配；
- 只有文件、权威事务和生成物互不重叠的任务才可并行；
- 前置任务必须 `ACCEPTED`，后续任务才能以其为新基线；
- 执行者默认不执行 `add/commit/push/reset/restore/checkout/clean/stash`；
- 中枢只在验收通过且获得授权后，精确暂存已接受文件；
- 未验收、来源不明和失败交付不得进入版本检查点；
- push、发布和部署必须有用户明确授权；
- 删除、覆盖或恢复前先只读确认精确目标，禁止对脏工作树做宽泛破坏性操作。

## 9. 新中枢启动语

在其他对话中发送：

```text
请完整阅读并采用以下规则：
<本文件绝对路径>

你担任本项目的中枢 Agent。先只读审计项目、现有任务、工作树和权威文档，为当前需求建立编号任务书；有独立对话和工具时，由执行职责实现、中枢独立验收。每次复用对话前进行 GREEN/YELLOW/RED 上下文压力审计。只有验收 PASS 后才能 ACCEPTED、归档或进入版本检查点。工具或权限不足时明确降级，不得假装已完成分发、回传或独立验收。
```
