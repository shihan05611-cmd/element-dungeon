# AI 协作中枢运行协议（极简通用版）

## 最小流程

```text
用户目标 → 中枢按需派发 → 执行者实现并自测 → Review 最小充分验收
                                      ├─ PASS → DONE
                                      ├─ FAIL → 返回修复
                                      └─ BLOCKED → 说明所缺条件
```

Review Agent 是最终验收者。`PASS` 立即等同于最终接受；中枢只负责跟踪、协调和通知结果，不重复验收，不另行执行 `ACCEPTED`。

## 最小任务书

```markdown
# <任务名>

目标：<要得到什么结果>
范围：<可改范围；无特殊边界时一句话即可>
完成：<怎样算完成>
```

只有在不写会造成歧义或风险时，才增加依赖、禁止项或必要背景。任务书不要求固定编号、状态表、Review 等级、线程信息、模型参数、逐文件清单、推导过程、证据包或固定交付模板。

## 三方职责

- **中枢**：澄清关键目标，做必要拆分，选择执行与 Review，处理阻塞和范围冲突。
- **执行者**：理解任务，在范围内实现，运行最相关的测试，简要交付改动、验证和残余风险。
- **Review**：独立核对需求和实际结果，以足以判断正确性的最小验证给出结论；不顺手修复，除非另行接到实现任务。

## 验收与安全

- 验收强度由 Review 按实际风险决定。默认不要求冷副本、全量回归、全库哈希、独立 profile、截图矩阵或 evidence 包；只有它们对当前结论确有必要时才做。
- 视觉改动看实际画面，行为改动跑直接相关测试，高风险公共契约才扩大回归。
- 用户当前明确决定优先于旧任务书和历史记录；仓库实际状态优先于聊天记忆。
- 保留无关工作树改动。未经明确授权，不执行破坏性 Git 恢复、push、发布或部署。
