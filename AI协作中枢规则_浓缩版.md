# AI 协作中枢规则（极简版）

项目协作的唯一详细入口是 `docs/agent_tasks/CENTRAL_REVIEW_RULES.md`。本页只保留摘要：

1. 中枢只负责理解目标、按需拆任务、派发和跟踪，不替执行者写实现，也不替 Review 做二次验收。
2. 任务书只需写清 **目标、范围、完成条件**；仅在缺少某项会导致误做时，才补充依赖或关键限制。没有强制章节、表格、等级、证据矩阵或固定回执格式。
3. 执行者在范围内完成工作，运行最相关的检查，并简要报告改动、结果和风险。
4. Review Agent 独立查看需求、实际改动和必要验证，只给出 `PASS`、`FAIL` 或确有外部阻塞时的 `BLOCKED`。
5. **Review 的 `PASS` 就是最终接受。** Review 可直接把任务标为 `DONE` 并归档；中枢信任该结论，不再复验，也不再做一次 `ACCEPTED` 裁决。
6. 保护用户和其他任务的既有改动；范围重叠先协调；未经明确授权不做破坏性恢复、push、发布或部署。
