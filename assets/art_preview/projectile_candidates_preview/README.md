# 敌人元素弹体候选 · 能力验证预览

状态：**PREVIEW ONLY / 未接入**。本目录为「敌人子弹重构」议题的美术可行性验证样品，**未去背、未压到目标尺寸、未接入任何场景、脚本或资源**，不构成交付物，也不改变 `boss_arc_projectile` 的现役采纳状态。

生成方式：内置 `image_gen`，`stylized-concept` 单次生成，未使用 CLI/API/model fallback。提示词照抄 `assets/generated/vfx/boss_arc_projectile/prompt.md` 的成功模板结构。

## 文件

| 文件 | 用途 | 源尺寸 | 目标显示 | 字节 | SHA-256 |
|---|---|---:|---:|---:|---|
| `fire_bolt_candidate_v1_chroma.png` | 火元素敌人弹（左向） | 1024×1024 | 约 64×32 | 907,089 | `964DAC3931B20F904A52BA0375F50D5A8EA7F6A54ED265E67C4360AA7CFAF9DF` |
| `water_bolt_candidate_v1_chroma.png` | 水元素敌人弹（左向） | 1024×1024 | 约 64×32 | 893,965 | `8A53988964F8D5A019A5FA6093A7A73DAB29A354E3A24FC1FAC9580DB78047E8` |

两张均为 `#00ff00` 色键背景，尚未执行去背。

## 规范自检（对照 `docs/art/像素美术规范_v1.md` §9）

| 检查项 | 火弹 | 水弹 |
|---|---|---|
| 强水平剪影（2:1） | 通过 | 通过 |
| 明度档 ≤5 | 通过（4 档） | 通过（4 档） |
| 单一主高光簇 | 通过 | 通过 |
| 左上光源 | 通过 | 通过 |
| 硬边阶梯像素 | 通过 | 通过 |
| 描边色相符合 §4.1 | 通过（深棕紫，火域） | 通过（深蓝黑，水域） |
| 无文字/水印/阴影/光晕/背景 | 通过 | 通过 |
| 无角色/手/握柄/拾取闪光 | 通过 | 通过 |
| 尾鳍细节颗粒度 | 通过 | **偏碎**，正式化时需并为 2 个大块 |

## 正式化尚缺的步骤

1. 色键去背（`#00ff00` → alpha），含 despill 与 edge-contract；
2. 缩到目标逻辑尺寸并做**整数缩放**校验（禁止 §2.2 的非整数比例）；
3. 在目标尺寸下手工像素清理（水弹尾鳍并块）；
4. 在暗色/水色/火色/紫色四种背景上做轮廓可读性检查；
5. 生成 Godot `.import` sidecar 并登记 manifest。

未修改任何正式 PNG、场景、脚本、资源或项目设置。
